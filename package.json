{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "setup:fresh": "composer run setup:fresh",
        "sync": "composer install && npm install && php artisan migrate && php artisan db:seed && php artisan config:clear && php artisan route:clear && php artisan view:clear",
        "sync:docker": "docker compose exec water_billing_app composer install && npm install && docker compose exec water_billing_app php artisan migrate && docker compose exec water_billing_app php artisan db:seed && docker compose exec water_billing_app php artisan config:clear && docker compose exec water_billing_app php artisan route:clear && docker compose exec water_billing_app php artisan view:clear",
        "deploy": "vite build && php artisan migrate --force",
        "deploy:fresh": "vite build && php artisan migrate:fresh --seed --force"
    },
    "devDependencies": {
        "@alpinejs/collapse": "^3.15.4",
        "@tailwindcss/forms": "^0.5.2",
        "@tailwindcss/vite": "^4.0.0",
        "alpinejs": "^3.4.2",
        "autoprefixer": "^10.4.2",
        "axios": "^1.11.0",
        "baseline-browser-mapping": "^2.9.8",
        "concurrently": "^9.0.1",
        "laravel-echo": "^2.3.0",
        "laravel-vite-plugin": "^2.0.0",
        "postcss": "^8.4.31",
        "pusher-js": "^8.4.0",
        "tailwindcss": "^3.1.0",
        "vite": "^7.0.7"
    },
    "dependencies": {
        "@fontsource/inter": "^5.2.8",
        "@fontsource/merriweather": "^5.2.11",
        "@fontsource/outfit": "^5.2.8",
        "@fortawesome/fontawesome-free": "^7.2.0",
        "chart.js": "^4.5.1",
        "html2pdf.js": "^0.14.0",
        "html5-qrcode": "^2.3.8",
        "jsqr": "^1.4.0",
        "signature_pad": "^5.1.3",
        "xlsx": "^0.18.5"
    }
}
