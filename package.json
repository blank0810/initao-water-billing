{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "sync": "composer install && npm install && php artisan migrate && php artisan config:clear && php artisan view:clear && npm run build",
        "sync:docker": "composer install && npm install && docker compose exec water_billing_app php artisan migrate && docker compose exec water_billing_app php artisan config:clear && docker compose exec water_billing_app php artisan view:clear && npm run build",
        "deploy": "vite build && php artisan migrate --force",
        "deploy:fresh": "vite build && php artisan migrate:fresh --seed --force"
    },
    "devDependencies": {
        "@alpinejs/collapse": "^3.15.4",
        "@tailwindcss/forms": "^0.5.2",
        "@tailwindcss/vite": "^4.0.0",
        "alpinejs": "^3.4.2",
        "autoprefixer": "^10.4.2",
        "axios": "^1.11.0",
        "baseline-browser-mapping": "^2.9.8",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "postcss": "^8.4.31",
        "tailwindcss": "^3.1.0",
        "vite": "^7.0.7"
    },
    "dependencies": {
        "chart.js": "^4.5.1"
    }
}
